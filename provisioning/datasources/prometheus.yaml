apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: $__env{PI_PROMETHEUS_URL}
    isDefault: true
    editable: true
    jsonData:
      timeInterval: 5s
